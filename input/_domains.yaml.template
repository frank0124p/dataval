# Domain 描述範本（input domain template）
# ───────────────────────────────────────────────
# 用途：描述「這次要驗證的設計關聯到哪些 domain」，
#       讓工具只載入相關 domain 的 skill（Common 一律載入）。
#
# 兩種用法：
#  (A) 進階：每個 DDL 各自描述
#      把這個檔複製成  <你的DDL名>.domains.yaml
#      例如  part_master.sql  →  part_master.domains.yaml
#
#  (B) 簡單：整批共用
#      把這個檔改名成  _domains.yaml （拿掉 .template），
#      input/ 裡所有 DDL 都套用這份設定。
#
# 若兩者都沒有：只載入 Common，並在報告發出 domain 未指定警告。
# 未知 domain 會被略過並顯示於報告。
# 指定的 domain 也會決定要參照哪些 production/<domain>/*.sql 命名基準；
# 工具不會掃描未指定的 production domain。

# 這份設計關聯到的 domain（對應 config/skills/ 底下的資料夾名）
domains:
  - PLM
  - FCM

# 自由描述（給人與 agent 看的，不影響程式判斷）
description: |
  在此用一句話描述這份資料設計屬於哪個業務領域、與哪些 domain 有關聯。
  例如：本設計為 PLM 的料件主檔與 BOM，並與 FCM 的主檔資料有跨域關聯。
