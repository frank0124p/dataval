# domain 參考 ER 模型(Mermaid `.mmd`)

放本領域「核心實體與關係」的參考模型。驗證 DDL 時,凡「兩端表都出現在
本次 DDL」的關係會被納入比對(LINEAGE.ER_SUGGESTION),提示宣告與模型的出入。
格式推薦 Mermaid:純文字可 diff、GitHub / VS Code 直接渲染成圖,
比 JSON/YAML 對人友善;工具內建解析器,不需外部相依。
